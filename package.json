{
  "name": "flamingo-ui",
  "version": "0.0.1",
  "description": "transaction history component",
  "main": "src/index.js",
  "scripts": {
    "start": "budo web/page.js:bundle.js --dir ./ --live --open -- -i STATE",
    "build": "browserify web/page.js -i STATE -o bundle.js",
    "start-legacy": "budo web_legacy.js:bundle.js --live --host 0.0.0.0",
    "build-legacy": "browserify web_legacy.js > bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ibrarli/flamingo-wallet.git"
  },
  "author": "Ibrar",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "budo": "^11.8.4",
    "standardx": "^7.0.0",
    "browserify": "^17.0.1"
  }
}